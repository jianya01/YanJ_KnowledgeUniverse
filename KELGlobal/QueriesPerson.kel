/* Queries specific to the Person Entity definition belong here. */
IMPORT KELGlobal.Entities, KELGlobal.EntityUses; //, KELGlobal.Attributes;

// Basic query to dump a random sample of Person ENTITY contents
QUERY: DumpAllPersonData <= Person;

// Basic query to dump the Person ENTITY contents for a given LexID
QUERY: DumpPersonData(SearchForLexID) <= Person(UID = SearchForLexID);